name: "Minimal Test Scenario"
description: "Minimal scenario for integration testing"
architecture: "x86_64"
tags: ["minimal", "test"]

vms:
  - name: "test-vm-minimal"
    memory: "512"
    vcpus: 1
    bootOrder: ["network"]

resources: []

assertions:
  - type: "dhcp_lease"
    vm: "test-vm-minimal"
    description: "VM should obtain DHCP lease"

timeouts:
  dhcpLease: "10s"
  vmProvision: "30s"

expectedOutcome:
  status: "passed"
  description: "Minimal test passes"
